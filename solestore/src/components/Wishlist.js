import React from 'react'
import { useEffect } from "react";

const Wishlist = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  });
  return (
    <div>Wishlist</div>
  )
}

export default Wishlist